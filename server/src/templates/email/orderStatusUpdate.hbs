<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Order Update - {{orderNumber}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #17a2b8; color: white; padding: 30px 20px; text-align: center; border-radius: 8px 8px 0 0; }
        .content { padding: 30px 20px; background: white; }
        .status-update { background: #d4edda; border: 1px solid #c3e6cb; padding: 20px; margin: 20px 0; border-radius: 5px; border-left: 4px solid #28a745; }
        .status-shipped { background: #cce5ff; border-color: #007bff; border-left-color: #007bff; }
        .status-delivered { background: #d1ecf1; border-color: #17a2b8; border-left-color: #17a2b8; }
        .status-cancelled { background: #f8d7da; border-color: #dc3545; border-left-color: #dc3545; }
        .tracking-info { background: #fff3cd; border: 1px solid #ffeaa7; padding: 15px; margin: 15px 0; border-radius: 5px; }
        .button { display: inline-block; padding: 15px 30px; background: #007bff; color: white; text-decoration: none; border-radius: 5px; font-weight: bold; }
        .footer { padding: 20px; text-align: center; color: #666; font-size: 12px; background: #e9ecef; border-radius: 0 0 8px 8px; }
        .status-icon { font-size: 48px; margin-bottom: 15px; }
        .progress-bar { background: #e9ecef; height: 8px; border-radius: 4px; margin: 20px 0; overflow: hidden; }
        .progress-fill { background: #28a745; height: 100%; transition: width 0.3s ease; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="status-icon">
                {{#if (eq status 'shipped')}}🚚{{/if}}
                {{#if (eq status 'delivered')}}📦{{/if}}
                {{#if (eq status 'processing')}}⚙️{{/if}}
                {{#if (eq status 'confirmed')}}✅{{/if}}
                {{#if (eq status 'cancelled')}}❌{{/if}}
            </div>
            <h1>Order Status Update</h1>
            <p>{{orderNumber}}</p>
        </div>
        <div class="content">
            <h2>Hello {{firstName}},</h2>
            <p>We have an update on your order!</p>
            
            <div class="status-update {{#if (eq status 'shipped')}}status-shipped{{/if}}{{#if (eq status 'delivered')}}status-delivered{{/if}}{{#if (eq status 'cancelled')}}status-cancelled{{/if}}">
                <h3> Status: {{status}}</h3>
                <p><strong>{{statusMessage}}</strong></p>
                
                {{#if trackingNumber}}
                <div class="tracking-info">
                    <h4> Tracking Information</h4>
                    <p><strong>Tracking Number:</strong> {{trackingNumber}}</p>
                    {{#if estimatedDelivery}}
                    <p><strong>Estimated Delivery:</strong> {{estimatedDelivery}}</p>
                    {{/if}}
                </div>
                {{/if}}
            </div>
            
            {{#if (eq status 'shipped')}}
            <div style="background: #e3f2fd; padding: 20px; margin: 20px 0; border-radius: 5px;">
                <h4> Your package is on its way!</h4>
                <p>Your order has been handed over to our shipping partner and is now in transit. You can track its progress using the tracking number above.</p>
                <ul>
                    <li> Track your package in real-time</li>
                    <li> You'll receive delivery notifications</li>
                    <li> Contact the carrier for delivery preferences</li>
                </ul>
            </div>
            {{/if}}
            
            {{#if (eq status 'delivered')}}
            <div style="background: #d1ecf1; padding: 20px; margin: 20px 0; border-radius: 5px;">
                <h4>🎉 Your order has been delivered!</h4>
                <p>We hope you love your new tech! If you have a moment, we'd appreciate your feedback.</p>
                <ul>
                    <li> Leave a product review</li>
                    <li> Share photos of your purchase</li>
                    <li> Browse our latest arrivals</li>
                </ul>
            </div>
            {{/if}}
            
            {{#if (eq status 'cancelled')}}
            <div style="background: #f8d7da; padding: 20px; margin: 20px 0; border-radius: 5px;">
                <h4> Order Cancelled</h4>
                <p>Your order has been cancelled. If you didn't request this cancellation, please contact our support team immediately.</p>
                <p>Any charges will be refunded to your original payment method within 3-5 business days.</p>
            </div>
            {{/if}}
            
            <p style="text-align: center; margin: 30px 0;">
                <a href="{{trackingUrl}}" class="button">View Order Details</a>
            </p>
            
            <p>Need help? Our customer service team is available 24/7 to assist you.</p>
        </div>
        <div class="footer">
            <p>&copy; {{year}} {{companyName}}. All rights reserved.</p>
            <p>Questions? Contact us at <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></p>
        </div>
    </div>
</body>
</html>