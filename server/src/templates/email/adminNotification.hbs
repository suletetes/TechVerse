<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>[TechVerse Admin] {{subject}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #6c757d; color: white; padding: 30px 20px; text-align: center; border-radius: 8px 8px 0 0; }
        .content { padding: 30px 20px; background: white; }
        .notification-box { background: #f8f9fa; border-left: 4px solid #007bff; padding: 20px; margin: 20px 0; border-radius: 0 5px 5px 0; }
        .critical { border-left-color: #dc3545; background: #f8d7da; }
        .warning { border-left-color: #ffc107; background: #fff3cd; }
        .info { border-left-color: #17a2b8; background: #d1ecf1; }
        .success { border-left-color: #28a745; background: #d4edda; }
        .metadata { background: #e9ecef; padding: 15px; margin: 15px 0; border-radius: 5px; font-size: 12px; }
        .button { display: inline-block; padding: 15px 30px; background: #007bff; color: white; text-decoration: none; border-radius: 5px; font-weight: bold; }
        .footer { padding: 20px; text-align: center; color: #666; font-size: 12px; background: #e9ecef; border-radius: 0 0 8px 8px; }
        .admin-icon { font-size: 48px; margin-bottom: 15px; }
        .priority-high { border: 2px solid #dc3545; }
        .priority-medium { border: 2px solid #ffc107; }
        .priority-low { border: 2px solid #28a745; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="admin-icon">🔧</div>
            <h1>Admin Notification</h1>
            <p>TechVerse Management System</p>
        </div>
        <div class="content">
            <div class="notification-box {{#if (eq priority 'critical')}}critical{{else if (eq priority 'warning')}}warning{{else if (eq priority 'success')}}success{{else}}info{{/if}}">
                <h3>
                    {{#if (eq priority 'critical')}}🚨{{else if (eq priority 'warning')}}⚠️{{else if (eq priority 'success')}}✅{{else}}ℹ️{{/if}}
                    {{subject}}
                </h3>
                <p>{{message}}</p>
                
                {{#if details}}
                <div style="margin-top: 15px;">
                    <strong>Additional Details:</strong>
                    <ul>
                        {{#each details}}
                        <li>{{this}}</li>
                        {{/each}}
                    </ul>
                </div>
                {{/if}}
            </div>
            
            {{#if userId}}
            <div class="metadata">
                <h4>📊 Event Metadata</h4>
                <table style="width: 100%; font-size: 12px;">
                    {{#if userId}}
                    <tr>
                        <td style="padding: 4px 8px 4px 0; font-weight: bold;">User ID:</td>
                        <td style="padding: 4px 0;">{{userId}}</td>
                    </tr>
                    {{/if}}
                    {{#if userEmail}}
                    <tr>
                        <td style="padding: 4px 8px 4px 0; font-weight: bold;">User Email:</td>
                        <td style="padding: 4px 0;">{{userEmail}}</td>
                    </tr>
                    {{/if}}
                    {{#if ip}}
                    <tr>
                        <td style="padding: 4px 8px 4px 0; font-weight: bold;">IP Address:</td>
                        <td style="padding: 4px 0;">{{ip}}</td>
                    </tr>
                    {{/if}}
                    {{#if endpoint}}
                    <tr>
                        <td style="padding: 4px 8px 4px 0; font-weight: bold;">Endpoint:</td>
                        <td style="padding: 4px 0;">{{method}} {{endpoint}}</td>
                    </tr>
                    {{/if}}
                    {{#if userAgent}}
                    <tr>
                        <td style="padding: 4px 8px 4px 0; font-weight: bold;">User Agent:</td>
                        <td style="padding: 4px 0; word-break: break-all;">{{userAgent}}</td>
                    </tr>
                    {{/if}}
                    <tr>
                        <td style="padding: 4px 8px 4px 0; font-weight: bold;">Timestamp:</td>
                        <td style="padding: 4px 0;">{{timestamp}}</td>
                    </tr>
                </table>
            </div>
            {{/if}}
            
            {{#if actionUrl}}
            <p style="text-align: center; margin: 30px 0;">
                <a href="{{actionUrl}}" class="button">Take Action</a>
            </p>
            {{/if}}
            
            <div style="background: #e3f2fd; padding: 15px; margin: 20px 0; border-radius: 5px;">
                <h4>🛠️ Admin Quick Actions</h4>
                <ul style="margin: 10px 0; padding-left: 20px;">
                    <li><a href="{{baseUrl}}/admin/dashboard">View Admin Dashboard</a></li>
                    <li><a href="{{baseUrl}}/admin/users">Manage Users</a></li>
                    <li><a href="{{baseUrl}}/admin/orders">Review Orders</a></li>
                    <li><a href="{{baseUrl}}/admin/products">Manage Products</a></li>
                    <li><a href="{{baseUrl}}/admin/analytics">View Analytics</a></li>
                </ul>
            </div>
            
            {{#if (eq priority 'critical')}}
            <div style="background: #f8d7da; border: 1px solid #dc3545; padding: 15px; margin: 20px 0; border-radius: 5px;">
                <h4>🚨 Critical Alert - Immediate Action Required</h4>
                <p>This notification indicates a critical system event that requires immediate attention. Please review and take appropriate action as soon as possible.</p>
            </div>
            {{/if}}
        </div>
        <div class="footer">
            <p>&copy; {{year}} {{companyName}} - Administrative System</p>
            <p>This is an automated admin notification. Do not reply to this email.</p>
            <p>Generated at {{timestamp}}</p>
        </div>
    </div>
</body>
</html>